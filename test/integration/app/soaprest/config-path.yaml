apiVersion: app/v1
kind: SOAPRESTMiddleware
metadata:
  name: test
  namespace: testNamespace
spec:
  matcher:
    patterns: ["/soap/"]
    matchType: Exact
  simpleMethodSpec:
    trimSpace: true
    preferShort: true