version: 1

goos: freebsd
goarch: 386

main: ./cmd/aileron/
binary: aileron-{{ .Os }}-{{ .Arch }}

env:
  - CGO_ENABLED=0

flags:
  - -trimpath

ldflags:
  - "-w"
  - "-s"
  - '-extldflags "-static"'
  - "-X main.version={{ .Env.VERSION }}"
