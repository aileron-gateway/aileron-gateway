version: 1

goos: linux
goarch: arm

main: ./cmd/aileron/
binary: aileron-{{ .Os }}-{{ .Arch }}

env:
  - CGO_ENABLED=0
  - GOARM=7

flags:
  - -trimpath

ldflags:
  - "-w"
  - "-s"
  - '-extldflags "-static"'
  - "-X main.version={{ .Env.VERSION }}"
